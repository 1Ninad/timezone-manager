@* Home.razor
   Redirects the root URL "/" to the New Entry page.
   NavigationManager.NavigateTo works in both SSR (server redirect) and interactive mode. *@

@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/new-entry");
    }
}
