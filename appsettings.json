{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Set the real connection string in appsettings.Development.json (gitignored, never committed).
    // Mac + Docker example:  "Server=localhost;Database=TimezoneManager;User Id=sa;Password=<password>;TrustServerCertificate=True;"
    // Mac + corporate server: ask your manager for the full connection string.
    // Windows:               "Server=localhost;Database=TimezoneManager;Trusted_Connection=True;TrustServerCertificate=True;"
    "DefaultConnection": ""
  }
}
